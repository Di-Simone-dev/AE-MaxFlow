/**
 * @file fraction.cpp
 * @brief File di implementazione della classe `Fraction`.
 *
 * L'implementazione è interamente inline nel file `fraction.hpp`:
 * il costruttore, tutti gli operatori aritmetici e i metodi di conversione
 * sono definiti direttamente nel corpo della classe, come di consueto per
 * classi piccole e self-contained senza dipendenze esterne complesse.
 *
 * Questo file esiste per convenzione di progetto (ogni `.hpp` ha un `.cpp`
 * corrispondente) ma non contiene codice compilabile.
 *
 * @see fraction.hpp
 *
 */

#include "fraction.hpp"

// Nessuna implementazione qui: tutto inline nel .hpp
// (È normale: classi piccole e template-like si implementano nel .hpp)